version: '3'
services:
  postgres:
    image: postgres:9.6
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=springbootformation
      - POSTGRES_USER=roberttaloise
      - POSTGRES_PASSWORD=123456
      - MAX_CONNECTIONS=300